#include <bits/stdc++.h>
using namespace std;

struct Node {
int data;
Node* next;
}

void Print (Node* p) {
if (p == NULL)
{
return;
}

cout<<p->data<<endl;
Print(p->next);
}








int main()
{
Node* head = NULL;
.
.
.}
